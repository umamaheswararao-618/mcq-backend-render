# =====================================================================================
# APPLICATION CONFIGURATION FOR RENDER DEPLOYMENT
# =====================================================================================
#
# All configuration for this application is managed via environment variables
# in the deployment platform (Render). This ensures that no sensitive data like
# passwords are stored in the code.
#
# Spring Boot will automatically detect and use the corresponding
# environment variables set on the Render dashboard.
#
